{
  "name": "railflow-website",
  "private": true,
  "type": "module",
  "description": "RailFlow landing page. Static: no build step, no dependencies. three.js loads from jsDelivr at runtime.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "serve": "python -m http.server 5500"
  },
  "engines": {
    "node": ">=18"
  }
}
